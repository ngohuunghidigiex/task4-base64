export enum EButtonType {
    Default = 'default',
    Hover = 'hover',
    Clicked = 'clicked',
    Disabled = 'disabled',
    Processing = 'processing',
}

export enum ButtonSize {
    Small = 'sm',
    Medium = 'md',
    Large = 'lg',
}

export enum IconPossiton {
    Left = 'left',
    Right = 'right',
    None = 'none',
}

export enum ETheme {
    Dark = 'dark',
    Light = 'light',
    Text = 'text',
}
